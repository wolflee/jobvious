<p>
  <strong>Title:</strong>
  <%= @job.title %>
</p>

<p>
  <strong>Location:</strong>
  <%= @job.location %>
</p>

<p>
  <strong>Description:</strong>
  <%= @job.description %>
</p>

<p>
  <strong>Contact:</strong>
  <%= @job.contact %>
</p>

<p>
  <strong>Company:</strong>
  <%= @job.company %>
</p>

<p>
  <strong>Url:</strong>
  <%= @job.url %>
</p>

<% if current_user == @job.user %>
<%= link_to 'Edit', edit_job_path(@job)%> |
<% else %>
<div>
  <% if @applied %>
  Already applied, if you continue, the previous one will be renewed.
  <%= link_to 'Apply Anyway!', new_job_application_path(@job) %>
  <% else %>
  <%= link_to 'Apply!', new_job_application_path(@job) %>
  <% end %>
</div>
<% end %>
<% # please use css to control '|' %>
<%= link_to 'Back', jobs_path %>
